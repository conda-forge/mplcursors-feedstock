{% set name = "mplcursors" %}
{% set version = "0.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/m/mplcursors/mplcursors-{{ version }}.tar.gz
  sha256: a4dda539931ff961498f98bd35386bf345954c6235849eb8981728d479b099a9

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
    - setuptools_scm
  run:
    - python >={{ python_min }}
    - matplotlib-base >3.1

test:
  requires:
    - python {{ python_min }}
  imports:
    - mplcursors

about:
  home: https://github.com/anntzer/mplcursors
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: mplcursors provides interactive data selection cursors for Matplotlib. It is inspired from mpldatacursor, with a much simplified API.
  doc_url: https://mplcursors.readthedocs.io/
  dev_url: https://github.com/anntzer/mplcursors

extra:
  recipe-maintainers:
    - hmaarrfk
